name = "xcode_triager"
description = "Apple build, test, scheme, simulator, and log triage profile."
model = "gpt-5.6-terra"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"
developer_instructions = "Gather build and runtime evidence before proposing edits. Do not change code until the parent asks."
